# Diff manifest — Phase 1a (11 июня 2026)

Три низкорисковых порта в maksim-prod (правило Phase 1a: только то, что улучшает Максима).

## 1. Feature: Avatar IV через /v3 (движение тела/рук)
- **Source:** panferov-legacy (content-bot, 7 июня): `heygen_v3_avatar_video` + call-site 3 пути + кнопка v2→v4
- **Target:** bot.py — новая `heygen_v3_avatar_video` (после `heygen_generate_video`), кнопка брендового пикера «Avatar 4» `heygen_ver:v2`→`v4`, call-site: ветка `elif avatar_version == "v4"` → /v3 type:avatar (expressiveness=high + brand motion_prompt) + `_use_v3_polling=True`
- **Зачем Максиму:** его «Avatar 4» слал legacy v2 → клиент получал «только голову» вместо Avatar IV с жестами
- **Deps:** HEYGEN_API_KEY, существующий v3-polling (`heygen_v3_check_status`), `BRANDS[brand].get("heygen_motion_prompt")` (опциональный ключ, дефолт-промпт в коде)
- **Flag:** нет (фикс маппинга, не новая фича)
- **Tests:** py_compile; схема /v3 подтверждена реальным submit на panferov 7.06; ручная приёмка Артёмом на panferov (движение рук)
- **Rollback:** revert коммита (кнопка вернётся на v2)

## 2. Feature: Brand switch fix (стейл card_brand)
- **Source:** panferov-legacy (6 июня), Telethon-регресс там scenarios/24
- **Target:** bot.py `brand_set:` handler — `_brand_ctx.set("")` + `pending.pop("card_brand")`
- **Зачем Максиму:** латентный баг форка (один бренд сейчас, всплывёт при втором); /brand скрыт, но callback живой
- **Deps:** нет
- **Rollback:** revert (3 строки)

## 3. Fix: callback-коллизия broll_rm (review vs легаси)
- **Source:** собственный баг maksim-prod (найден агентом 10 июня, чип)
- **Target:** bot.py — review-слой «Моё выбранное» переименован `broll_rm:<gi>`→`brv_rm:<gi>` (кнопка `_broll_review_text_kb` + handler); легаси `broll_rm:<card>:<file>` получил guard на stale-кнопки старого формата (len parts < 3 → «кнопка устарела»)
- **Зачем:** легаси-удаление файлов проекта падало ValueError (int-парсер review перехватывал)
- **Tests:** tests/test_broll_review.py обновлён (brv_rm + анти-коллизионный ассерт) — ALL PASS
- **Rollback:** revert

## Прогон
- py_compile OK; test_broll_review / test_card_broll_save / test_broll_toggle — ALL PASS
- pre_deploy: 4 старых известных FAIL (F841 final_line, card_menu callbacks, smoke _project_dir — зафиксированы в аудите 04.06, НЕ от этих портов)
