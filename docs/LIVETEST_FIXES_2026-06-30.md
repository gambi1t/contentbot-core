# Live-test фиксы Илон — план работ (2026-06-30)

> Durable-план. Источник правды для этой серии правок. Обновляется по ходу.
> Worktree: `D:\AI\maksim-bot-panferov` (ветка `work/panferov`). Старт HEAD `e329efa`.
> Сервер Илон: `65.21.154.237`, сервис `contentbot-core` (+ `contentbot-core-telethon`), путь `/root/contentbot-core/`.

## Режим работы
- **СТРОГО ПОСЛЕДОВАТЕЛЬНО**, по одному фиксу: Rule №2 (3 вопроса + альтернативы + почему лучше) → **ALL-PLACES** (найти ВСЕ точки входа) → **TDD-тест первым** → реализация → роль-ревьюер (баги) + security review → деплой (дрейф-чек CRLF→LF + бэкап + scp + пре-флайт `import bot` + рестарт NRestarts=0 + проверка логов) → пуш.
- Каждая строка кода ↔ требование. Без drive-by рефакторинга и «на всякий случай».
- **Максима НЕ трогаем** (бренд-изоляция). Все правки brand-aware, default=Илон/panferov.
- Не уверен в факте/строке — проверяю кодом или пишу «не уверен». Не гадать.

## Источник диагнозов
- Диагностика (10 агентов): workflow `wnxp5rsps` → `C:\Temp\claude\D--AI\...\tasks\wnxp5rsps.output`.
- ALL-PLACES карта (5 агентов + критик): workflow `w8cjmg6js` (в работе на момент написания) → когда придёт, вписать полные списки мест в каждый пункт ниже.

---

## Порядок и статус

| # | Фикс | Решение Артёма | Статус |
|---|------|----------------|--------|
| 0 | ALL-PLACES аудит (read-only) | — | ✅ готов (wf w8cjmg6js + критик) |
| 1 | P1: ссылка на скачивание в чат (>48МБ) | R1 ✅ ссылка в чат | ⬜ |
| 2 | Корень: гидрация сценария из Notion (P2b) | R2b ✅ гидрация на открытии карточки | ⬜ |
| 3 | Корень: текст не утекает в «новую идею» (P2a) | рекомендация (без явного спора) | ⬜ |
| 4 | Корень: мёртвая кнопка на фото (P3-краш) | R3 ✅ + все места | ⬜ |
| 5 | P3-остальное: правка текста обложки + единый ключ | R3 ✅ + все места | ⬜ |
| 6 | P4: правка субтитров ДО прожига (реюз селфи) | R4 ✅ до ролика | ⬜ |
| 7 | P5: превью ИИ-монтажа ДО сборки | ⏳ ждёт confirm | ⬜ |
| 8 | P6: новый голос TG-поста во ВСЕ входы + точечная правка | live-test 30.06 | ⬜ |
| 9 | P7: «Назад к выбору площадок» мёртвая (crosspost) | live-test 30.06 | ⬜ |

---

## P1 — ролик 84МБ: «забери из Notion», а файла нет
**Решение:** R1 — отдать nginx-ссылку на скачивание прямо в чат, формулировку «Забери из Notion-карточки» убрать.
**Корень:** ветка `download_project` при >48МБ ([bot.py:20896](../bot.py)) шлёт только текст, в карточку ничего не пишет; файл в Notion не грузится вообще (file-upload API не используется). Тихие `try/except` прячут сбой записи ссылки.
**Реюз (Rule №2 Q3):** канон `_broll_deliver` ([bot.py:4150](../bot.py)) — `save_media_permanent(video)` → ссылка в чат. Скопировать паттерн.
**Места:** ветка `download_project` (≈20896-20899). MAX_BOT_UPLOAD=48МБ (20871). **ALL-PLACES:** проверить, нет ли других мест с «забери из Notion» / >лимита (вписать из аудита).
**Тест:** >48МБ → сообщение содержит рабочую ссылку, нет «Notion-карточки».

## Корень/P2b — `/cards` «Нет сценария», хотя в Notion есть
**Решение:** R2b — гидрировать `pending['script']` из Notion при ОТКРЫТИИ карточки (по образцу `card_continue`), чтобы все кнопки увидели сценарий. Сохранить честный гейт для случая «в Notion реально пусто».
**Корень:** `gen_description` ([bot.py:20470](../bot.py)) читает только `data['script']`; `notion_card:` ([bot.py:15832](../bot.py)) кладёт лишь id+title, не тянет сценарий.
**Где сценарий в Notion:** под заголовком «Сценарий» в теле страницы (подтверждено скрином Артёма) — ровно где читает `fetch_notion_page_script` ([bot.py:6259](../bot.py)).
**Реюз (Rule №2 Q3):** `card_continue` (≈14288 пишет `pending['script']` из Notion + честный гейт 14290), `card_*` группа (≈15644-15666), `tgpost_from_script` (≈20568).
**ALL-PLACES:** все open-точки карточки (notion_card:, card-чипы, «К карточке», idea_fork→карточка, /ready→карточка) + все кнопки-потребители `data['script']` (gen_description 20470, broll_shooting_list 19103). Полный список — из аудита.
**Тест:** открыть карточку с сценарием в Notion (без прохода через озвучку/бролл) → «Описание» работает; пустой Notion → честное «нет сценария».

## Корень/P2a — текст после «Готово» уходит в «Идея принята»
**Рекомендация:** на шаге описания свободный текст = пользовательское описание; централизованно сузить fallthrough «любой текст = новая идея».
**Корень:** `/ready` держит callback-only state `selfie_cover_picking`, которого нет в текстовом роутере; `finish` его не сбрасывает; текст падает в catch-all ([bot.py:10074](../bot.py)) → `_show_pipeline_fork`.
**ALL-PLACES:** полный список «висячих» состояний (выставляются, но не обрабатываются в `process_idea` 8275..10056) — из аудита. Нужен выверенный allowlist «меню-состояний», иначе сломаем быстрый ввод идеи.
**Тест:** текст на шаге описания → становится описанием/не уходит в idea-fork; обычный ввод новой идеи из чистого состояния по-прежнему работает.

## Корень/P3-краш — мёртвая кнопка на фото-сообщении
**Решение:** R3 — единый безопасный хелпер `edit_caption → edit_text → send_message`, применить ВО ВСЕХ местах, где `edit_message_text` зовётся на фото/медиа.
**Корень (доминанта):** `cover_redo_text` — `edit_message_text` на ФОТО, ВНЕ try ([bot.py:21435](../bot.py)) → BadRequest «no text to edit» → молчаливый `error_handler` (21903). Детерминированно на каждый клик.
**Реюз (Rule №2 Q3):** `edit_message_caption`-then-fallback в `cover_confirm` (≈21484), `answer+send_message` (≈18470).
**ALL-PLACES:** все небезопасные `edit_message_text`-на-медиа (cover_pick except 21387, cover_redo except 21477, cover_ok except 21899, guard data is None 16221, и др.) + `error_handler` уведомляет юзера. Полный список — из аудита. **НЕ регрессия 28.06** (баг с 13 июня).
**Тест:** клик «Другой текст обложки» на фото-превью → нет молчаливого падения, юзер видит ответ.

## P3-остальное — правка текста обложки + единый ключ
**Решение:** R3 — отдельная кнопка «изменить ТЕКУЩИЙ текст обложки» (не регенерация 5 вариантов) + единый источник `effective_cover_title` вместо рассинхрона `cover_text` vs `selfie_cover_text`. Во ВСЕХ cover-флоу.
**Корень:** подпись «🖼 Обложка: «{cover_text}»» читает легаси `cover_text`, который обнуляется (cover_notext='' / pop в cover_redo_text); selfie пишет текст в `selfie_cover_text` → пустые «».
**ALL-PLACES:** все cover-флоу (легаси/card 21379/21608/21893, selfie 7186, broll b2cov 1167-1338), все ключи, все точки ввода (state cover_approval 9884, ловушка-подстрока 9887, broll2_cover_text). Полный список — из аудита.
**Тест:** правка добавляет букву в текущий текст; «Обложка: «…»» не пустая.

## P4 — субтитры Аватар+B-roll: правка ДО прожига
**Решение:** R4 — pre-burn review транскрипта (реюз селфи-механизма), на ВСЕХ путях аватар/B-roll (не один экран).
**Корень:** правка субтитров есть только в селфи (pre-burn review). Пути аватар/broll жгут Whisper напрямую (words=None), слова выбрасывают, экрана/кнопки правки нет.
**Реюз (Rule №2 Q3):** селфи: states `selfie_text_review`/`selfie_text_editing` (selfie/handlers ≈539-625), `apply_user_edits` (selfie/edit.py, word-count-lock), `_burn_and_request_title` (≈681), `build_review_message`/`_review_keyboard`, запись `words.json` (bot.py ≈7953).
**ALL-PLACES:** все callsite `add_subtitles_to_video` с words=None: card_assemble→assemble_auto_montage (15485 / video_assembler 2347), avatar_publish (17192), standalone broll (broll/handlers 2362). Исключить dead-код (if False 9292). Полный список — из аудита.
**Открытое:** транскрипт для аватара — известный сценарий vs Whisper-аудио (forced alignment = отдельная опция, high-risk, не сейчас).
**Тест:** на аватар/broll-пути перед прожигом — экран правки транскрипта; правка отражается в субтитрах; прожиг один раз.

## P5 — превью ИИ-монтажа ДО сборки (ждёт confirm Артёма)
**Рекомендация:** для ИИ-монтажа (`card_asm_go:a`) вставить паузу-превью ДО рендера: показать план + «✅ Собрать так» / «✂️ Поправить раскладку». Правка пересчитывает ТОЛЬКО план (1 вызов Claude, без ffmpeg), показывает снова, по кругу; рендер — один раз по подтверждённому плану. Для детерминированного Про-монтажа паузу не делать.
**Сейчас:** план показывается ([bot.py:15380](../bot.py)) и СРАЗУ рендер; правка «✂️ Поправить раскладку» стоит на экране РЕЗУЛЬТАТА ([bot.py:15598](../bot.py)) → пересборка = второй рендер. Гонять дважды.
**Реюз (Rule №2 Q3):** `generate_montage_plan` + `montage_instruction` + state `montage_edit_waiting` (15196) уже есть — переставить момент.
**Тест:** ИИ-монтаж → план + кнопки подтверждения; «Поправить» меняет план без сборки; «Собрать так» = один рендер.

---

## Открытые решения
- **P5** — нужен явный «да» Артёма на превью-гейт ИИ-монтажа.
- **P3** (опц.) — нужно ли `error_handler` глобально уведомлять юзера об ошибке (усилитель «мёртвых кнопок»).

## ALL-PLACES карта — ФИНАЛ (аудит wf `w8cjmg6js` + критик полноты)
Полные file:line — в `C:\Temp\claude\...\tasks\w8cjmg6js.output`. Ниже — решённый подход + что доловил критик.

**P1** — единственная точка `download_project` >48МБ ([bot.py:20895-20899](../bot.py)). Реюз `_broll_deliver` ([bot.py:4120](../bot.py)) → nginx-ссылка в чат. Убрать «забери из Notion». Критик: карты для P1 в наборе не было, но корень подтверждён.

**P2b** — ЕДИНАЯ гидрация в `notion_card:` ([bot.py:15832](../bot.py), после 15847) по образцу `card_continue` ([bot.py:14305](../bot.py)): `pending['script']=fetch_notion_page_script(full_id)` (try/except, не валить меню) + `notion_page_id`. **Писать `script` БЕЗУСЛОВНО (вкл. '')** — иначе утечёт сценарий прошлой карточки. Критик снял сомнение: `notion_card:` — единственная open-точка (модули карточку не открывают; `card_script:` пишет `script_edit_card`, не `script`). Закрывает gen_description(20470)/broll_shooting_list(19103)/озвучку — все читают тот же pending.

**P2a** — ЦЕНТРАЛИЗОВАННЫЙ opt-in гейт в ОБА catch-all: текст [bot.py:10074](../bot.py) + голос [bot.py:10863](../bot.py). `_IDEA_INPUT_STATES={None,'','pipeline_fork}` → только из них текст/голос стартует идею; иначе вежливый отказ «жми кнопку / отмена». Общий хелпер для text+voice (DRY). Образец — существующий гейт `not state` ([bot.py:8748](../bot.py)). Критик доловил: (1) голос течёт ШИРЕ текста; (2) **ВИДЕО** в callback-only state падает в idea-fork с пустым `idea_text` → гейт должен также отбрасывать пустой текст/медиа; (3) +6 dangling-состояний (cover_pool_upload_waiting, heygen_photo_register_waiting, selfie_broll_uploading_photo, awaiting_selfvoice, awaiting_voiceover_ownvoice, broll2_ownvoice); (4) НЕ трогать `voice_editing` escape ([bot.py:9759](../bot.py)) — он сам делает `_generate_script` и возвращается раньше. **Открытое:** `selfie_covertext` ([bot.py:7204](../bot.py)) — введённый текст обложки нигде не читается → проверить, не латентный ли это баг, перед тем как просто отклонять.

**P3-крах** — расширить существующий `_send_or_edit` ([bot.py:2136](../bot.py)) до caption→text→send (образец `cover_confirm` 21484). Заменить unsafe edit на медиа: cover_redo_text(21435/21458/21470/21477), cover_notext(21397/21428), avatar_pick(21234/21257/21270/21277), guard data is None(16222). except-после-delete (cover_pick 21387, cover_ok 21899) → `answer+send_message`. Усилить `error_handler` ([bot.py:21903](../bot.py)) — уведомлять юзера. Критик: cover_photo/avatar_confirm/avatar_pick_by_number/cover_pool_upload уже безопасны — **НЕ трогать** (анти-drive-by). **Кросс с P2b:** `data['script']` по индексу в 21140/21247/21448 → `.get('script','')` + гейт пустого.

**P3-остальное** — единый `effective_cover_title(data)` + билдер `_cover_caption(title)` (пусто → «без текста (само фото)») на все 4 подписи (21379/21608/21893/7186) — убивает «Обложка: «»». Единый ключ `cover_title`: легаси `cover_text`, selfie — писать `cover_title` (удалить мёртвый `selfie_cover_text` 7180, ридеров нет), broll — `draft['cover_title']` ([broll/handlers.py:1334](../broll/handlers.py)). Кнопка «✏️ Изменить текущий текст» (state `cover_edit_waiting`) по образцу `desc_edit` ([bot.py:20693](../bot.py)) во все cover-превью; broll — нужен шаг превью ПЕРЕД терминальным `_finalize_broll_cover`. `cover_redo_text` (регенерация) оставить отдельно. **Открытое:** правка текста обложки в **selfie** (там текст=заголовок) — спросить Артёма.

**P4** — ОДИН общий pre-burn review (свой namespace `subrev:*`, своё состояние), реюз ЧИСТЫХ селфи-функций: `apply_user_edits` ([selfie/edit.py:17](../selfie/edit.py), word-count-lock), `build_review_message`, `detect_text_unchanged`. НЕ реюзать селфи-callback-хендлеры (приварены к селфи `_PENDING`). 3 пути → все через `add_subtitles_to_video(words=)`: card_asm_go при `+субтитры` без words.json (15492), avatar_publish (17192), broll (broll/handlers.py:2362). Whisper всё равно нужен для ТАЙМИНГОВ (как в селфи), правка = орфография под lock. Персист в `words.json` (образец 7953) + broll-draft (он персистится на диск — критик поправил карту). Критик подтвердил: ровно 3 burn-пути, скрытого 5-го нет.

## P6 — TG-пост пайплайна: старый формат + нет точечной правки (live-test 30.06)
**Симптом:** TG-пост, который генерится в конце пайплайна («📰 TG-пост по сценарию»), идёт СТАРЫМ форматом промптинга (Артём узнал свой старый стиль). И на экране результата нет «✂️ Точечная правка» — только «🔄 Перегенерировать».
**Корень (подтверждён грепом, проверить тело при реализации):**
- (a) `tgpost_from_script` ([bot.py:20608](../bot.py)) → `tg_post_writer.rewrite_for_telegram` ([tg_post_writer.py:960](../tg_post_writer.py)). Новый голос 28.06 (`SYSTEM_PROMPT_ARTEM` tg_post_writer.py:29 + `_polish_post_finale` :767) вшит в standalone `/tgpost`, а `rewrite_for_telegram` его, похоже, не зовёт → старый формат. **ALL-PLACES:** через `rewrite_for_telegram` идут ТРИ входа — пайплайн `tgpost_from_script` + `_broll_tg_post` ([bot.py:4185](../bot.py)) + crosspost-описание. Фикс в `rewrite_for_telegram` (единая точка) → новый голос везде. Бренд-aware (Максиму свой/узко).
- (b) Кнопки результата ([bot.py:8438-8440](../bot.py)): нет «✂️ Точечная правка». Реюз tgpost-surgical-редактора 28.06 (`_apply_tgpost_surg_edit` / `_TGPOST_SURG_EDITOR_SYSTEM_ARTEM`).
**Тест:** rewrite_for_telegram даёт текст в новом голосе (A/B как 28.06); на экране пайплайн-поста есть точечная правка и она работает.

## P7 — «Назад к выбору площадок» мёртвая в crosspost (live-test 30.06)
**Симптом:** Кросспостинг → «нет описания для публикации» (правильно) → кнопка «⬅️ Назад к выбору площадок» неактивна.
**Корень (подтвердить):** кнопка ([bot.py:17855](../bot.py)) шлёт `callback_data="crosspost"` (голый, без id), а хендлер ждёт `crosspost:{card_id}` → нет матча → тишина (НЕ исключение → сеть error_handler не помогает). Фикс: отдать `crosspost:{id}` (или сделать хендлер устойчивым к голому "crosspost" с подтягиванием card_id из pending). **ALL-PLACES:** проверить, нет ли других голых `"crosspost"`-кнопок.
**Тест:** «Назад к выбору площадок» возвращает на экран выбора площадок.

## Открытые решения (не блокируют P1)
- **P5** — ждёт «да» Артёма (превью ИИ-монтажа до сборки).
- **selfie_covertext** — латентный баг или мусор? Проверить кодом на шаге P2a.
- **Правка текста обложки в selfie** — нужна ли (текст=заголовок)? Спросить на шаге P3.

## Прогресс (коммиты на work/panferov, локально, НЕ задеплоено)
- ✅ `c8fca3b` P1 — ссылка в чат. TDD test_download_materials.
- ✅ `9c1a1f6` P2b — гидрация сценария в notion_card:. TDD test_card_script_hydration.
- ✅ `f1ad0dc` P2a — гейт idea-fork (текст+голос+видео). TDD test_idea_gate_p2a (5).
- ✅ `996ce96` P3-крах — _send_or_edit на 4 статус-edit + error_handler сеть. TDD test_dead_button_p3.
- Регрессия: 163 pass, 9 пре-существующих падений (проверено на baseline e329efa), 0 новых.

**Defer-хардненинг (под сетью error_handler, не блокер):** `data['script']` по индексу в cover-ветках (21180/21287/21386/21532) → при пустом сценарии может дать мусор/KeyError; except-ветки cover (Ошибка:{e}) на фото. Сейчас покрыты сетью error_handler (юзер видит ответ). Добить точечно при желании.

## Batch-2 прогресс (локально, НЕ задеплоено)
- ✅ `f4a38f4` P7 — «Назад к выбору площадок» несёт card_id. TDD test_crosspost_back_button_p7.
- ✅ `ad3e891` P6b — точечная правка TG-поста по сценарию (кнопка + state + реюз _apply_tgpost_surg_edit). TDD test_tgpost_script_surg_p6b.
- 🟡 **P6a (старый формат TG-поста) — НЕ код-баг.** Прочитал код: пайплайн `tgpost_from_script` и standalone «Telegram-версия описания» идут через ИДЕНТИЧНЫЙ `generate_post(video_companion)` + `SYSTEM_PROMPT_ARTEM` (новый камертон 28.06). Кодовой разницы нет. Расхождение = стейл-кэш ИЛИ тип `video_companion` структурно ≠ ЭТАЛОН A. **Ждёт voice-решение Артёма** (промпт молча не меняю — его зона). Моя первичная греп-оценка была неверной, исправил чтением.
- Регрессий нет (9 ключевых тестов зелёные).

## Открытые решения (блокируют точечно, не весь батч)
- **P6a voice** — хочет ли Артём, чтобы `video_companion` (пост-к-ролику) звучал как ЭТАЛОН A (личная подача)? Тогда подкручу шаблон tg_post_writer.py:508 + A/B. Или сначала пусть сравнит свежий пост (после 28.06) — возможно стейл.
- **P3-обложка / selfie** — правка текста обложки в selfie (там текст = заголовок карточки). Lead-dev call: НЕ добавляю кнопку правки в selfie (разведёт обложку и заголовок), делаю для легаси/card + broll. Артём может возразить.

## Регрессия-экспозиция P2b (аудит wf `wqv1n4cug`, 30.06)
P2b-гидрация `notion_card:` (script+page_id) открыла пути к downstream-коду, читающему поля по индексу. Стрельнул `card_data` (cover_pick/cover_confirm) — починен `.get`+фолбэк. Аудит: класс **исчерпан**, ещё одно место `data["idea"]`@16398 защищено превентивно (`03dc92e`).
**РЕШЕНИЕ: `notion_card:` НЕ добивать до полного набора `card_continue`** — высокий clobber-риск (срабатывает на каждом «◀️ К карточке»; безусловная установка card_data/state/voice_parts затрёт активный пайплайн).
**🔒 ИНВАРИАНТ:** `notion_card:` ставит только notion_edit_card/notion_edit_title/script/notion_page_id. Эталон полного открытия = `card_continue`. Любой НОВЫЙ downstream, читающий delta-поля (card_data, notion_url, idea, source_urls, youtube_urls, voice_parts, voice_approved, state) по индексу `[]` и достижимый с `notion_card:`-карточки — обязан `.get` с фолбэком.
**Давний край (вне класса, на будущее):** bot.py:16609 `voice_approved[idx]=False` → IndexError если короче voice_parts; выровнять при следующей правке озвучки.

## Codex external review (по запросу Артёма 30.06)
Codex (`codex exec -s read-only` на `e329efa..HEAD`) поймал то, что **in-house аудит пропустил**: не KeyError, а **stale-состояние между карточками** — `notion_card:` не чистил card-поля прошлой → `project_dir` берёт чужой `card_data.title` → не та папка. Фикс `_hydrate_card_context` (card-id-различие: другая карточка → очистка+гидрация, та же → WIP) — коммит `31836d7`. + P6b low (generic ошибка). Codex medium закрыты тем же фиксом; low (cover_approval отмена-trap) → #26. Память: [[feedback_external_codex_review_for_critical]].

## Текущая позиция
> **ВСЁ ЗАДЕПЛОЕНО на Илон + запушено. HEAD `31836d7`, NRestarts=0, логи чистые.** Сессия 30.06: P1, P2b, P2a, P3-крах (+review-фиксы), P7, P6b, card_data KeyError, idea-exposure, **context-switch (Codex)**. Бэкапы на сервере: bot.py.bak_predeploy_batch1/batch2.
> **Открыто:** P3-обложка (#26, + cover_approval отмена-trap) · P4 субтитры (#27) · P5 превью ИИ-монтажа (ждёт confirm) · P6a (Артём оставил как есть, A/B доказал новый голос) · давний край voice_approved[idx] (при правке озвучки).
