ЧЕСТНАЯ ОЦЕНКА ЗРЕЛОСТИ (по факту кода, перепроверено в D:\AI\maksim-bot).

ГЛАВНЫЙ ВЫВОД: цель «добавить клиента = конфиг + ассеты + чек-лист, ноль кода» (заявлена ключевым тестом архитектуры, docs/03_TENANT_MODEL.md:142-144) СЕГОДНЯ НЕ ДОСТИГНУТА. Не junior-solo. Реальная формула онбординга: РАЗРАБОТЧИК (код-пререквизиты) → ЧЕЛОВЕК (внешние сервисы + первичный логин Telethon) → ДЖУН (серверная механика по плейбуку). Джун выполняет установочную механику, но не весь онбординг.

КЛЮЧЕВАЯ ПОПРАВКА К ЧЕРНОВИКУ (была фактическая ошибка): для СОВЕРШЕННО НОВОГО клиента «путь Б без кода» НЕВОЗМОЖЕН в принципе, а не «редкий случай». brand_overrides (tenant.py:148-167) мёржится ТОЛЬКО поверх существующей записи BRANDS; нет записи → config_doctor валит «brand_overrides references unknown brand» (tenant.py:248). В BRANDS сейчас только default/shoes/maksim (bot.py:435/465/612). Значит новый клиент ВСЕГДА начинается с правки BRANDS разработчиком (🔴), и лишь потом джун донастраивает 11 плоских полей из allowlist (tenant.py:45-56). Черновик подавал большинство клиентов как «путь Б джуном» — это вводило в заблуждение.

JUNIOR-READY СЕГОДНЯ (🟢, по чек-листу, ПОСЛЕ код-пререквизитов разработчика):
- Сборка features{} из галочек, КАЖДАЯ явно true/false (валидируется config_doctor, tenant.py:229-233).
- Запись простых .env-полей (TELEGRAM_*, NOTION_DATABASE_ID, AUTHOR_*, MAKSIM_*-пути, DEFAULT_DM_REPLY_URL).
- brands.allowed[] (жёсткий гейт пикера, tenant.py:169-198).
- brand_overrides ТОЛЬКО для 11 плоских полей из allowlist — и только если бренд уже заведён в BRANDS.
- Каркас каталога, venv, rsync ассетов, копирование/правка tenant.json из tenants/maksim.example.json, установка юнитов из готовых шаблонов.
- Оба гейта (tenant doctor + cutover_doctor с --expected-instance <tenant>), старт, 15-мин watch.
- Нажать /yt_auth /ig_auth /vk_auth (но это ✋ — нужен доступ клиента и решённый redirect).

ВСЁ ЕЩЁ DEVELOPER-GRADE (🔴, джун застрянет):
1. Новый бренд = правка BRANDS-dict (bot.py:434+). Структурные поля (heygen_looks, platforms, notion_status_type, notion_rubrics, wardrobe_modes) вне allowlist (config_doctor: «key not in allowlist», tenant.py:256). Главный gap «zero-code»; закрывается только полным выносом BRANDS, отложенным на ПОСЛЕ Phase 3 (docs/03_TENANT_MODEL.md:102-104).
2. OAuth redirect ЗАШИТ на panferov-домен для ВСЕХ ТРЁХ площадок (crosspost.py:29 YouTube + переиспользуется для IG на :456; crosspost.py:1209 VK). Это БЛОКЕР для ЛЮБОГО нового клиента, а не только «со своим доменом» (важная поправка к черновику). Нужен общий студийный callback с маршрутизацией ИЛИ правка кода + регистрация redirect в консолях.
3. .service-шаблонов в репо НЕТ (только _xvfb.service) — первый раз генерирует разработчик из panferov-юнитов.
4. cutover_doctor._FOREIGN_MARKERS (cutover_doctor.py:22-26) и дефолт --expected-instance=panferovai (cutover_doctor.py:213) зашиты под panferov → ложные срабатывания. Нужен generic-режим.
5. notion_status_type select-vs-status — структурное решение схемы.
6. РФ AI-провайдер (152-ФЗ): слоя смены провайдера в коде НЕТ (docs/03_TENANT_MODEL.md:62-64) — RFC, не настройка.

ПРИНЦИПИАЛЬНО РУЧНОЕ (✋, скриптом не автоматизируется): BotFather, тренировка HeyGen-аватара, клон голоса ElevenLabs, создание Notion-базы+схемы, OAuth-consent, ПЕРВИЧНЫЙ логин Telethon под телефон клиента (для нового клиента ВСЕГДА — нет скопированной .session). Скрипт может лишь подставить уже добытые секреты.

РИСКИ ДЛЯ ДЖУНА (тихие сбои — все вынесены в DoD плейбука):
- Путь Б без записи в BRANDS → config_doctor «unknown brand». Не тратить время на tenant.json до завода бренда.
- Notion schema mismatch: расхождение имён код↔база молча плодит «левые» опции (docs/03_TENANT_MODEL.md:43-45), авто-чека нет — жёсткая ручная сверка 1:1.
- Per-feature .env-ключи разбросаны по 10+ модулям, gating fail-open (tenant.py:103-119): включил фичу, забыл ключ → молча не работает. Плюс не указал false → выключенная фича остаётся живой.
- MAKSIM_-префикс путей сбивает с толку: переменные ОБЯЗАТЕЛЬНЫ для каждого тенанта (paths.py:24/35/41/49), не пропускать из-за имени.
- Копирование чужого .env целиком → бот работает «под чужим». Только шаблон .env.maksim.template + свои значения.
- ai_video/Seedance без cost-guard = money-leak; параметры не заданы → owner_only до явного бюджета.
- secrets-файл без chmod 600 → утечка прав.

ДОРОЖНАЯ КАРТА АВТОМАТИЗАЦИИ (что заскриптовать, чтобы стало junior-proof; приоритет сверху):
A. Вынести BRANDS-dict в tenant.json целиком (~12 точек чтения) — снимает главный 🔴, делает путь Б реальным для новых клиентов (Phase 3+).
B. Параметризовать OAuth-redirect (общий callback-домен с маршрутизацией по tenant ИЛИ env-переменная redirect) + параметризовать cutover_doctor-маркеры/expected-instance — убрать panferov-хардкод.
C. Единый .env.example с разметкой required-всегда vs per-feature + авто-валидатор «фича включена → её ключ есть» (закрывает fail-open-грабли).
D. Авто-чек схемы Notion против бренд-конфига (сверка имён опций) — убирает молчаливый schema mismatch.
E. Положить в репо параметризуемые .service-шаблоны (bot+telethon) с плейсхолдером <tenant>.
F. provision_tenant.sh — оркестратор механики Части A.2+ и гейтов Части B (rsync→venv→tenant.json из example→.env-сборка→doctor→юниты).
Пока A–F не сделаны — честная формула остаётся: разработчик (бренд+redirect+юниты) → человек (внешние сервисы+Telethon-логин) → джун (механика по плейбуку).
